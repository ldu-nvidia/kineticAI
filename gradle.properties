org.gradle.jvmargs=-Xmx2048m -Dfile.encoding=UTF-8
android.useAndroidX=true
kotlin.code.style=official
android.nonTransitiveRClass=true

# Pin Gradle to the full JDK 17 that ships with jlink/javac.
# System java-21-openjdk-amd64 is a JRE (no compiler tools), which
# causes "jlink executable ... does not exist" at build time.
org.gradle.java.home=/home/ldu/android-dev/jdk-17.0.2
